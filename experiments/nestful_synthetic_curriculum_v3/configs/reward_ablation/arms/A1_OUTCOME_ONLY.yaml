# Reward arm A1_OUTCOME_ONLY — the ONLY key that legitimately differs from
# the other four arm files is `reward.train_policy`. Everything else comes
# from ../round1_base.yaml.
reward_id: A1_OUTCOME_ONLY
description: "Path-invariant terminal outcome only. No process tie-break (epsilon=0). Scientific control for the cost of a fully sparse reward."
reward:
  train_policy: reward_ablation_A1_OUTCOME_ONLY
wandb:
  run_name: A1_OUTCOME_ONLY_seed20260724
  extra_tags: [reward_ablation_A1_OUTCOME_ONLY]
