# Extra deps for the pilot2 bundle on top of the v3 trainer environment.
# The factory itself is pure-stdlib + pydantic; these are for reporting/eval.
pydantic>=2.0
PyYAML>=6.0
numpy>=1.24
scipy>=1.10
