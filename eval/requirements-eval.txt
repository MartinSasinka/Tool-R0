# Additional requirements for eval benchmarks.
# Install on top of the main Tool-R0 requirements.txt:
#   pip install -r eval/requirements-eval.txt

pyyaml>=6.0
huggingface_hub>=0.20.0
openai>=1.0.0

# Optional: official BFCL evaluation checker (for paper-grade results)
# pip install bfcl-eval
